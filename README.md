# AirlineApp — Приложение для учёта и поиска авиабилетов

🎬 **Описание проекта**  
AirlineApp — настольное Java-приложение для управления авиарейсами и учёта пользователей.  
Приложение позволяет:  
- Регистрировать пользователей и входить в систему;  
- Просматривать список авиарейсов;  
- Искать рейсы по городу и дате;  
- Администраторам — добавлять, редактировать и удалять рейсы;  
- Генерировать новые рейсы автоматически.  

Проект разработан как курсовая работа на Java с использованием Swing и Log4J.

---

🎯 **Цели проекта**  
- Создать удобное GUI-приложение для работы с авиарейсами;  
- Автоматизировать учет и хранение информации о пользователях и рейсах;  
- Реализовать безопасный вход и регистрацию пользователей;  
- Предоставить администраторам функционал управления рейсами.  

---

📝 **Задачи проекта**  
1. Разработка моделей: `User`, `AdminUser`, `RegularUser`, `Flight`.  
2. Реализация сервисов: `UserService`, `FlightService`.  
3. Создание утилит для работы с файлами: `FileUtil`, `FlightGenerator`.  
4. Разработка графического интерфейса с использованием Swing (`MainAppSwing`).  
5. Логирование действий пользователей через Log4J.  
6. Обеспечение поиска, фильтрации и сортировки рейсов.  

---

📌 **Структура проекта**

AirlineApp/
├─ src/ # Исходный код
│ ├─ model/ # Модели пользователей и рейсов
│ ├─ service/ # Сервисы для работы с данными
│ ├─ ui/ # Графический интерфейс
│ └─ util/ # Вспомогательные классы
├─ bin/ # Скомпилированные .class файлы
├─ lib/ # Внешние библиотеки (Log4J, JavaFX)
├─ flights.csv # Данные о рейсах
├─ users.csv # Данные о пользователях
├─ log4j2.xml # Конфигурация логгирования
├─ run.ps1 # Скрипт для компиляции и запуска
└─ README.md # Этот файл

yaml
Копировать код

---

🛠 **Используемые технологии и библиотеки**  

- Java 17+  
- Swing (GUI)  
- Log4J 2 (логирование)  
- JavaFX (опционально для графических элементов)  

---

💻 **Примеры кода**

**Добавление нового пользователя:**

```java
UserService userService = new UserService();
userService.register("oleg", "12345");
Поиск рейсов:

java
Копировать код
FlightService flightService = new FlightService();
List<Flight> flights = flightService.getAllFlights().stream()
        .filter(f -> f.getDeparture().equals("Алматы"))
        .collect(Collectors.toList());
Редактирование рейса (для админа):

java
Копировать код
Flight flight = flights.get(0);
flight.setPrice(75000);
flightService.updateFlight(flight);
📦 Запуск проекта

Скомпилируйте проект через run.ps1 или вручную через javac.

Убедитесь, что все библиотеки из lib/ подключены.

Запустите MainAppSwing.java или через скрипт run.ps1.

✅ Итоги проекта

Реализован полноценный GUI с регистрацией и поиском рейсов;

Администраторы могут управлять рейсами;

Используется сохранение данных в CSV;

Добавлено логирование действий пользователей.
